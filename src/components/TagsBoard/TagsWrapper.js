import styled from 'styled-components';

const TagsWrapper = styled.div`
  margin: 10px 0;
  width: 100%;
  min-height: 100px;
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
`;

export default TagsWrapper;
